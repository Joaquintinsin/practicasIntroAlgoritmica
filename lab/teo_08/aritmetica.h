#ifndef ARITMETICA_H 
// usamos #ifndef para definir el identificador de interfaz (en este caso ARITMETICA_H)
// #ifndef es un método de compilación condicional que significa "si está definido"

// seccion de INTERFAZ (declaraciones publicas)
// a continuacion se declaran los perfiles de lo 
// que puede ser utilizado desde los programas que incluyan a la biblioteca arithmetica

int esPar(int n);
int factorial(int n);

#endif // fin del identificador e interfaz
