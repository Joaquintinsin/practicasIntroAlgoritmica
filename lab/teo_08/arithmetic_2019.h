#ifndef ARITHMETIC_2019_H // #ifndef se usa para definir el identificador (en este caso ARITHMETIC_H)

// seccion de Interfaz (declaraciones publicas)
// a continuacion se declaran los perfiles de las funtion que podrán ser utilizadas desde los programas que incluyan a la biblioteca arithmetic
int is_prime( int n );
int division( int dividend, int divisor, int * quotient );
int factorial( int n );

#endif // fin del identificador e interfaz
